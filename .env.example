# Copy this file to .env (gitignored) and fill in your values.
# The application falls back to the defaults shown in application.yml if these are not set.

# PostgreSQL
DB_URL=jdbc:postgresql://localhost:5432/evdiag
DB_USERNAME=evuser
DB_PASSWORD=evpass

# MQTT broker
MQTT_BROKER_URL=tcp://localhost:1883
